checkout:
  post:
  - chmod g-w Casks/*
dependencies:
  override:
  - git clone https://github.com/Linuxbrew/brew.git ~/.linuxbrew
  - mkdir -p ~/.linuxbrew/Caskroom
test:
  override:
  - ~/.linuxbrew/bin/brew cask audit ./Casks/*
